package Day5;

public class Animal {

    //Inheritance : allows one class (child/subclass) to acquire the properties and method of another class (parent/superclass)
    // java does not support multiple inheritance - Single Inheritance => in future "Interface"
    // Sub/Child => Reuse parent and method && i can own my fields and methods
    // Access modifiers => only public and protected are inhertance , not private

    public void eat() {
        System.out.println("Animal is eating food");

    }
}
